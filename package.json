{
  "name": "npm-gh-pages-template",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --minify --format=esm --outfile=dist/bundle.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "0.25.3",
    "gh-pages": "^6.3.0"
  }
}
